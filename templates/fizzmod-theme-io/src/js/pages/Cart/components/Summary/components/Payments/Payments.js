import React from 'react';

import styles from './styles';

const Payments = () => (
	<styles.Wrapper>
		<styles.Text variant="overline">pagá a través de</styles.Text>
		<styles.Mp />
	</styles.Wrapper>
);

export default Payments;
